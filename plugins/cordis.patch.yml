# dsh-mobile-ui 移动端插件(本地, 位于 ~/.dsh/profiles/node_modules/dsh-mobile-ui/):
#   UA 含 DSHM/ 的壳内才激活; 浏览器/原版零影响(dsh 本体保持纯净)
# ⚠ 带 client 的包 exports 必须含 "." 与 "./package.json"
# ⚠ id 全树唯一; 用 dshm- 前缀避免与上游裸 id 撞名
- insert:
    - id: dsh-mobile-ui
      name: 'dsh-mobile-ui'
