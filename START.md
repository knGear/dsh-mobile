# dshm — 移动端体验需求规格（跑分题）

> 本题只描述**需求与验收**，不提供任何实现线索。从零设计并实现一个可用的解决方案。
> 平台/机制细节需要你自行调研与决策。
> 注: 任务监视器（x/y 胶囊 + 任务面板）已独立为另一插件，**不在本题范围**。

## 背景

dsh（DeepSeek Harness）是 DeepSeek 官方的 AI 编码/任务框架，其 Web UI 运行在本机
`http://127.0.0.1:3080`。需求：为 dsh 提供完整的**移动端体验**——一个 WebView 壳 APK +
服务端本地 UI 插件，使手机成为 dsh 的一等客户端：能部署、调试、连接 dsh，并在手机上
获得适配的 Web UI 与快捷操作。

## 技术边界（环境事实，非实现提示）

- Android 8+（minSdk 26），WebView 壳；目标设备同时装有 Termux（终端模拟器）
- dsh 本体**不可修改**；只允许：①壳内注入 ②以本地插件形式扩展服务端/页面
- 插件体系分两侧：服务端进程侧（HTTP 路由）与浏览器页面侧（DOM/UI 注入）
- 兼容无 root 设备（root 只作为可选增强）
- 部署 dsh 有两条路径：Termux 原生（性能）与 Termux 内 Ubuntu（兼容/依赖完整）

## 功能需求

### A. 壳（APK）

1. **冷启动连接引擎**：打开 App 即尝试连接本机 dsh（默认 127.0.0.1:3080）。连接成功 → 加载
   dsh Web UI；失败 → 进入引导页。启动即用，无需用户手动操作。
2. **引导页**（壳内离线/初始合一页面，支持三种视角）：
   - 初来乍到（新用户）
   - 本机 dsh 未运行（曾用但当前离线）
   - dsh 已连接/已运行（展示运行状态）
3. 引导页内容（自上而下）：
   - 标题（三态文案）
   - 下载 Termux + 部署 dsh：两个部署按钮（Termux 原生 / Termux-Ubuntu），行末一个 "?" 按钮；
     点 "?" 弹出部署方式对比表（两列，逐项对比，每项更优的一方打 ✓ 标注）
   - 已安装？运行 dsh-web：一个可一键拉起/复制命令的入口
   - 连接：IP:端口 输入框 + 连接按钮；空输入=本机默认，只输 IP 自动补默认端口
   - 历史连接：成功连接过的地址以可点小标签留存（上限 5，最新在前，点击直连）
   - 遇到困难？：四档自救（浏览器打开原版 / 原版验证安装 / 简易重装 / 清洁重装）
   - 两个仓库链接（项目仓库 / dsh 官方仓库）
   - 可选：通知授权按钮
4. 引导页按钮交互：青色按钮支持 点击=复制命令（就地"已复制 ✓"反馈）与
   长按=拉起 Termux 执行；链接按钮用系统浏览器打开；纯按钮直接动作。
5. 部署对比表：Termux 原生 vs Termux-Ubuntu 两列，覆盖核心/性能/权限/稳定/安全/兼容/
   部署难度/维护升级等维度，每行更优的方案打 ✓，表尾给选型建议。
6. 连接能力：输入合法 IP:端口 后跳转加载该地址的 dsh（可连接远程实例）；历史连接持久化。
7. 全面屏：内容避开状态栏/挖孔/底部手势条；提供开关。
8. UA 标识：壳内 WebView 的 User-Agent 带唯一标识（如 DSHM/版本），供 UI 插件识别
   "这是本壳"从而激活移动适配；浏览器/其他环境不受影响。

### B. 移动适配 UI 插件（壳内激活）

> 仅当 UA 含壳标识时激活；浏览器/原版零注入零影响。dsh 本体保持纯净。

1. 移动设置面板（设置里一个独立分区，如"移动设置"）：
   - 进入引导页按钮（调试/回归引导页）
   - 排障区（横线分隔，含）：
     - 拉起浏览器：用系统浏览器打开当前 dsh 页面
     - 纯净模式开关：开启后禁用全部移动适配（只保留该开关本身，保证能切回，不得死锁）
     - 全面屏优化开关
     - 目录选择器·原始界面开关：切换用系统自带目录选择器还是插件提供的移动版
     - ui 自定义（可折叠子区）：正文空位、设置侧栏宽度等滑杆（持久化）
     - 布局预览：初始页/离线页两种排版预览入口
   - 版本检查：排障区下方独立区块（横线分隔，与排障同级）：显示当前 dsh / 壳版本与
     远程最新版本，有新版时提供更新入口；无新版置灰。
2. 重载/重启按钮（侧栏底部，普通版）：
   - 点击 = 重载前端（刷新页面，服务不重启、数据不丢）
   - 长按 = 重启：弹确认窗（标题 + 两个按钮），确认后执行，界面给"转圈 + 倒计时 + 就绪"反馈
   - 侧栏展开时显示"刷新/重启"文字，收起时只显示图标
3. 移动版目录选择器：添加工作区时用移动友好的目录列表（从 /sdcard 等可访问根起步，
   权限受限时提示普通权限；有 root 时可浏览完整文件系统）；支持新建目录与选择；
   顶部提供切回系统原始界面的入口。
4. 界面适配：移动端布局优化（设置侧栏宽度、正文区、统计信息条等按移动尺寸适配）。

### C. 多插件共存

系统里可能同时安装本 UI 插件与任务监视器插件（后者提供进阶版重载/重启按钮）：
- 两者都装时，监视器插件的进阶版接管重载/重启按钮，本插件的普通版按钮不显示；
  但本插件的其他功能（移动设置面板等）不受影响。
- 只装本插件时，普通版按钮正常工作。

## 验收要点

- 冷启动：装了 dsh → 直接进 UI；没装/离线 → 引导页，一键复制/拉起部署命令可用
- 引导页三态标题正确；对比表两列每行更优 ✓；历史连接可点直连
- 设置面板：纯净模式开启后只剩开关本身且能切回；滑杆持久化；版本检查独立区块
- 重载/重启：点击刷新、长按确认重启、转圈倒计时反馈完整
- 目录选择器从 /sdcard 起步、可新建、可切原始界面
- 手机浏览器打开 dsh（无壳标识）→ 零移动注入；壳内 → 移动适配全部生效
- 与监视器插件共存时只有一个重载/重启按钮（进阶版接管）
