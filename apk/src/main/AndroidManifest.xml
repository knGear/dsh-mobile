<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dsh.mobile"
    android:versionCode="54"
    android:versionName="0.054">

    <uses-sdk android:minSdkVersion="17" android:targetSdkVersion="34" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="com.termux.permission.RUN_COMMAND" />
    <!-- App 内更新: 安装未知应用(Android 8+ 需用户授权"安装此来源") -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask"
            android:exported="true"
            android:theme="@style/ShellTheme"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- dshm://back = Termux 跳回; dshm://first = debug 布局预览(设置-移动端) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="dshm" android:host="back" />
                <data android:scheme="dshm" android:host="first" />
            </intent-filter>
        </activity>

        <!-- 引导页(设置-移动端入口): 独立透明页, 返回/✕ 销毁回设置 -->
        <activity
            android:name=".GuideActivity"
            android:exported="true"
            android:theme="@style/GuideTheme"
            android:excludeFromRecents="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="dshm" android:host="guide" />
            </intent-filter>
        </activity>

        <!-- App 内更新: 暴露下载的 APK 给系统安装器(content:// URI, 无 androidx FileProvider 替代) -->
        <provider
            android:name=".ApkProvider"
            android:authorities="com.dsh.mobile.files"
            android:exported="false"
            android:grantUriPermissions="true" />

    </application>
    <!-- Android 11+ 包可见性: 使 Termux(含改名 fork)对 resolveService 可见 -->
    <queries>
        <package android:name="com.termux" />
        <intent>
            <action android:name="com.termux.RUN_COMMAND" />
        </intent>
    </queries>
</manifest>
